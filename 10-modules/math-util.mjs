const square = n => n*n ;
const add = (a,b) => a+b ;
const sqrt = n => Math.sqrt(n);

export {square,add,sqrt}

// Modules can be exported in two ways
// 1. Using named export
// 2. Using default export