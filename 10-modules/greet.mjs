const greetUser = name => console.log(`Hello ${name} , Welcome to the Session on JS Module`);
const generatePattern = () => console.log(`************************`);


export default greetUser;
export {generatePattern}
